

// sql => structured query language (to interact with relational database) (RDbms)

// 1986 - 1987

// mysql => open-source (RDBMS)

// myssql => web application 
// easier to integrate with backend

// mysql data types 

// Number - > int, bigint, decimal, float, double
// String -> char(fixed-length string) , varchar (variable-length), text(large text data), ENUM(one vaalue from predefined list)
// date & time -> date, time, datetime , year , timestamp
// special data types ->  Boolean, blob (binary data (ImageTrackList, files)), json 

// fiit -> char(8) -> default size 8 

// f i i t 0 0 0 0


// fiit -> varchar(20)

// f i i t 0 











